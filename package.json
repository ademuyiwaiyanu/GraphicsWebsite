{
  "scripts": {
    "install": "npm --prefix graphicswebsite install",
    "dev": "npm --prefix graphicswebsite run dev",
    "build": "npm --prefix graphicswebsite run build",
    "lint": "npm --prefix graphicswebsite run lint",
    "preview": "npm --prefix graphicswebsite run preview"
  },
  "devDependencies": {
    "playwright": "^1.57.0"
  }
}
